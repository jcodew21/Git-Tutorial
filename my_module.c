#include <stdio.h>

// 사칙연산 프로그램

void Add() // 더하기
{
	int a, b;
	a + b;
}

void Sub() // 빼기
{
	int a, b;
	a - b;
}

void Mul() // 곱하기
{
	int a, b;
	a * b; // 주석
}

void Div() // 곱하기
{
	int a, b;
	a / b;
}

int main() 
{
	Add();
	Sub();
	Mul();
	Div();

	return 0;
}